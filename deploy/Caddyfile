# Caddy auto-provisions and renews a Let's Encrypt certificate for $DOMAIN,
# and transparently proxies WebSockets (game traffic) to the app container.
{$DOMAIN} {
	encode zstd gzip
	reverse_proxy app:80
}
